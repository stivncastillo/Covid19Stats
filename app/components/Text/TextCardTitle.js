import styled from 'styled-components/native';

const TextCardTitle = styled.Text`
  color: ${props => props.theme.textCardTitle};
  text-transform: uppercase;
  font-weight: bold;
  margin-bottom: 16px;
`
export default TextCardTitle;
