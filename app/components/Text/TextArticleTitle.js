import styled from 'styled-components/native';

const TextArticleTitle = styled.Text`
  color: ${props => props.theme.text};
  font-weight: bold;
  margin-bottom: 4px;
`
export default TextArticleTitle;
