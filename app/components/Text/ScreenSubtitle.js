import styled from 'styled-components/native';

const ScreenSubtitle = styled.Text`
  color: ${props => props.theme.textCardTitle};
  width: 100%;
  text-transform: uppercase;
  margin-bottom: 16px;
`
export default ScreenSubtitle;
